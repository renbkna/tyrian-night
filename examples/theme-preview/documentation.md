# Theme preview documentation

The _preview corpus_ keeps quieter material pigmented while preserving navigational landmarks.

Inspect [browser runtime](src/browser_runtime.rs) when comparing file navigation in editor chrome.
