> indicator,
@@ -95,7 +107,7 @@ radiobutton:checked > indicator,
 radiodelegate:checked > indicator,
 menuitem:checked > indicator {
   background-color: var(--tyrian-indicator-checked-bg);
-  border-color: var(--tyrian-border-hover);
+  border-color: var(--tyrian-focus-ring);
 }
 
 checkbox:visual-focus > indicator,
@@ -109,22 +121,22 @@ menuitem:visual-focus > indicator {
 switch > indicator,
 switchdelegate > indicator {
   width: 36px;
-  height: 12px;
+  height: 16px;
   background-color: var(--tyrian-indicator-bg);
   border: 1px solid var(--tyrian-border-color);
-  border-radius: 6px;
+  border-radius: var(--tyrian-pill-radius);
 }
 
 switch:checked > indicator,
 switchdelegate:checked > indicator {
   background-color: var(--tyrian-indicator-checked-bg);
-  border-color: var(--tyrian-border-hover);
+  border-color: var(--tyrian-focus-ring);
 }
 
 switch:checked > indicator > handle,
 switchdelegate:checked > indicator > handle {
   background-color: var(--tyrian-selection-text);
-  border-color: var(--tyrian-border-hover);
+  border-color: var(--tyrian-focus-ring);
 }
 
 checkbox:hovered > indicator,
@@ -154,9 +166,9 @@ menuitem.exclusive > indicator {
   border-radius: var(--tyrian-pill-radius);
 }
 
-checkbox:checked indicator,
-checkdelegate:checked indicator,
-menuitem:checked indicator {
+checkbox:checked > indicator,
+checkdelegate:checked > indicator,
+menuitem:checked > indicator {
   icon-name: "checkmark-symbolic";
   icon-color: var(--tyrian-selection-text);
 }
@@ -182,11 +194,11 @@ switchdelegate:visual-focus > indicator > handle {
   outline: 2px solid var(--tyrian-focus-ring);
 }
 
-checkbox.changed indicator,
-checkdelegate.changed indicator,
-radiobutton.changed indicator,
-radiodelegate.changed indicator,
-switch.changed indicator {
+checkbox.changed > indicator,
+checkdelegate.changed > indicator,
+radiobutton.changed > indicator,
+radiodelegate.changed > indicator,
+switch.changed > indicator {
   background-color: var(--tyrian-neutral-bg);
   border: 1px solid var(--tyrian-neutral);
 }
diff --git a/source/union-css/parts/70-menus-popups.css b/source/union-css/parts/70-menus-popups.css
index 830bf84..c08bce5 100644
--- a/source/union-css/parts/70-menus-popups.css
+++ b/source/union-css/parts/70-menus-popups.css
@@ -5,18 +5,24 @@ menu {
 
 menuitem {
   width: var(--tyrian-menu-width);
-  border-radius: var(--tyrian-corner-radius);
+  border-radius: var(--tyrian-radius-medium);
 }
 
 menuitem:hovered,
 menuitem.with-submenu:highlighted {
-  background-color: var(--tyrian-hover);
-  border: 1px solid var(--tyrian-accent);
+  background-color: var(--tyrian-row-hover-bg);
+}
+
+menuitem:highlighted {
+  background-color: var(--tyrian-row-selected-bg);
 }
 
 menuitem:pressed {
-  background-color: var(--tyrian-accent);
-  color: var(--tyrian-selection-text);
+  background-color: var(--tyrian-control-active-bg);
+}
+
+menuitem:visual-focus {
+  outline: 2px solid var(--tyrian-focus-ring);
 }
 
 menuitem > indicator,
@@ -37,14 +43,13 @@ menuitem > shortcuttext {
 
 popup.combobox {
   padding: 0px;
-  background-color: var(--tyrian-panel-bg);
+  background-color: var(--tyrian-panel-raised-bg);
 }
 
 popup.combobox itemdelegate:highlighted,
 popup.combobox itemdelegate:highlight,
 popup.combobox menuitem:highlighted {
   background-color: var(--tyrian-row-selected-bg);
-  border-color: var(--tyrian-focus-ring);
 }
 
 overlay.modal,
@@ -70,25 +75,31 @@ menubaritem {
   height: var(--tyrian-medium-element);
   padding: var(--tyrian-small-spacing);
   spacing: var(--tyrian-small-spacing);
-  border-radius: var(--tyrian-corner-radius);
+  border-radius: var(--tyrian-radius-medium);
   color: var(--tyrian-foreground);
 }
 
-menubaritem:hovered,
+menubaritem:hovered {
+  background-color: var(--tyrian-row-hover-bg);
+}
+
 menubaritem:pressed,
 menubaritem:highlighted {
-  background-color: var(--tyrian-hover);
-  border: 1px solid var(--tyrian-focus-ring);
+  background-color: var(--tyrian-control-active-bg);
+}
+
+menubaritem:visual-focus {
+  outline: 2px solid var(--tyrian-focus-ring);
 }
 
 dialog {
   spacing: var(--tyrian-small-spacing);
   padding: var(--tyrian-large-spacing);
   border: 1px solid var(--tyrian-border-color);
-  border-radius: var(--tyrian-corner-radius);
-  background-color: var(--tyrian-panel-bg);
+  border-radius: var(--tyrian-radius-large);
+  background-color: var(--tyrian-panel-raised-bg);
   color: var(--tyrian-foreground);
-  box-shadow: 0px 2px 32px 0px rgba(0, 0, 0, 0.3);
+  box-shadow: var(--tyrian-shadow-elevation);
 }
 
 dialog .header {
@@ -125,6 +136,6 @@ tooltip {
   padding: var(--tyrian-large-spacing);
   color: var(--tyrian-foreground);
   background-color: var(--tyrian-panel-raised-bg);
-  border-radius: var(--tyrian-corner-radius);
+  border-radius: var(--tyrian-radius-large);
   border: 1px solid var(--tyrian-border-color);
 }
diff --git a/source/union-css/parts/80-containers-navigation.css b/source/union-css/parts/80-containers-navigation.css
index e13fa75..3511797 100644
--- a/source/union-css/parts/80-containers-navigation.css
+++ b/source/union-css/parts/80-containers-navigation.css
@@ -18,7 +18,7 @@ toolbar.footer {
 drawer.modal,
 overlaydrawer.modal {
   color: var(--tyrian-foreground);
-  background-color: var(--tyrian-window-bg);
+  background-color: var(--tyrian-panel-bg);
 }
 
 drawer[edge="left"],
@@ -41,10 +41,7 @@ overlaydrawer[edge="bottom"] {
   border-top: 1px solid var(--tyrian-border-color);
 }
 
-swipeview.horizontal {
-  layout-alignment: content fill fill 0;
-}
-
+swipeview.horizontal,
 swipeview.vertical {
   layout-alignment: content fill fill 0;
 }
@@ -60,7 +57,7 @@ scrollview.horizontal-scroll {
 scrollview.framed-background {
   background-color: var(--tyrian-panel-bg);
   border: 1px solid var(--tyrian-border-color);
-  border-radius: var(--tyrian-corner-radius);
+  border-radius: var(--tyrian-radius-large);
 }
 
 splitview handle {
@@ -69,6 +66,11 @@ splitview handle {
   background-color: var(--tyrian-border-color);
 }
 
+splitview handle:hovered,
+splitview handle:pressed {
+  background-color: var(--tyrian-border-hover);
+}
+
 splitview.horizontal handle {
   margin-left: var(--tyrian-medium-spacing);
   margin-right: var(--tyrian-medium-spacing);
@@ -90,10 +92,14 @@ pageindicatordelegate {
   background-color: var(--tyrian-muted);
 }
 
-pageindicatordelegate:pressed {
+pageindicatordelegate:hovered {
   background-color: var(--tyrian-border-hover);
 }
 
+pageindicatordelegate:pressed {
+  background-color: var(--tyrian-control-active-bg);
+}
+
 pageindicatordelegate.current {
   background-color: var(--tyrian-fill-bg);
 }
@@ -102,7 +108,7 @@ frame,
 groupbox {
   padding: var(--tyrian-small-spacing);
   border: 1px solid var(--tyrian-border-color);
-  border-radius: var(--tyrian-corner-radius);
+  border-radius: var(--tyrian-radius-large);
 }
 
 groupbox {
diff --git a/source/union-css/parts/90-sliders-progress-scrollbars.css b/source/union-css/parts/90-sliders-progress-scrollbars.css
index 5933ede..2af0a06 100644
--- a/source/union-css/parts/90-sliders-progress-scrollbars.css
+++ b/source/union-css/parts/90-sliders-progress-scrollbars.css
@@ -5,7 +5,7 @@ dial {
   height: 20px;
   background-color: var(--tyrian-track-bg);
   border: 1px solid var(--tyrian-border-color);
-  border-radius: var(--tyrian-corner-radius);
+  border-radius: var(--tyrian-pill-radius);
 }
 
 busyindicator {
@@ -20,7 +20,7 @@ slider > fill,
 rangeslider > fill {
   background-color: var(--tyrian-fill-bg);
   border: 1px solid var(--tyrian-border-hover);
-  border-radius: var(--tyrian-corner-radius);
+  border-radius: var(--tyrian-pill-radius);
 }
 
 progressbar.indeterminate > track {
@@ -57,18 +57,23 @@ scrollindicator[orientation="vertical"] {
 slider:visual-focus > handle,
 rangeslider > handle:active-focus,
 selectionrectanglehandle:visual-focus {
-  border-color: var(--tyrian-focus-ring);
   outline: 2px solid var(--tyrian-focus-ring);
 }
 
+slider:hovered > handle,
+rangeslider > handle:hovered,
+scrollbar > handle:hovered,
+scrollindicator > handle:hovered {
+  background-color: var(--tyrian-control-hover-bg);
+  border-color: var(--tyrian-border-hover);
+}
+
 slider:pressed > handle,
 rangeslider > handle:pressed,
 scrollbar > handle:pressed,
-scrollbar > handle:hovered,
-scrollindicator > handle:pressed,
-scrollindicator > handle:hovered {
+scrollindicator > handle:pressed {
   background-color: var(--tyrian-fill-bg);
-  border-color: var(--tyrian-border-hover);
+  border-color: var(--tyrian-focus-ring);
 }
 
 slider.horizontal,
@@ -120,7 +125,11 @@ scrollbar.noninteractive {
   height: 10px;
 }
 
-dial:hovered,
+dial:hovered {
+  background-color: var(--tyrian-control-hover-bg);
+  border-color: var(--tyrian-border-hover);
+}
+
 dial:pressed {
   background-color: var(--tyrian-control-active-bg);
   border-color: var(--tyrian-focus-ring);
diff --git a/source/union-css/parts/95-tabs.css b/source/union-css/parts/95-tabs.css
index 5642c1d..0bdbadc 100644
--- a/source/union-css/parts/95-tabs.css
+++ b/source/union-css/parts/95-tabs.css
@@ -1,20 +1,29 @@
 tabbutton {
   height: 32px;
   padding: var(--tyrian-medium-spacing) var(--tyrian-large-spacing);
+  background: none;
+  border: 1px solid transparent;
+  color: var(--tyrian-muted);
+}
+
+tabbutton:hovered {
   background-color: var(--tyrian-control-hover-bg);
-  border: 1px solid var(--tyrian-border-color);
+  color: var(--tyrian-foreground);
+}
+
+tabbutton:pressed {
+  background-color: var(--tyrian-control-active-bg);
   color: var(--tyrian-foreground);
 }
 
 tabbutton:checked {
   background-color: var(--tyrian-window-bg);
-  border-top: 3px solid var(--tyrian-focus-ring);
+  border-top: 2px solid var(--tyrian-focus-ring);
+  color: var(--tyrian-foreground);
 }
 
-tabbutton:hovered,
-tabbutton:pressed {
-  background-color: var(--tyrian-control-active-bg);
-  border-color: var(--tyrian-focus-ring);
+tabbutton:visual-focus {
+  outline: 2px solid var(--tyrian-focus-ring);
 }
 
 tabbutton:disabled {
@@ -43,7 +52,7 @@ tabbar[tab-position="bottom"] {
 
 tabbarhighlight.itemview-highlight {
   background-color: var(--tyrian-fill-bg);
-  border-radius: var(--tyrian-corner-radius);
+  border-radius: var(--tyrian-radius-small);
 }
 
 tabbutton.itemview-highlight {
diff --git a/tests/colorScience.test.ts b/tests/colorScience.test.ts
index 956e1e0..38a2acb 100644
--- a/tests/colorScience.test.ts
+++ b/tests/colorScience.test.ts
@@ -92,12 +92,12 @@ test('candidate ranking penalizes role-pair risk separately from nearest distanc
   const nocturne = readSourceTheme(
     SOURCE_THEMES.find((source) => source.slug === 'tyrian-nocturne')!
   );
-  const ranked = rankCandidates(nocturne, ['#109BB4', '#B957D0'], {
+  const ranked = rankCandidates(nocturne, ['#E09A6D', '#B957D0'], {
     neighbors: themeRoleColors(nocturne).filter((neighbor) => neighbor.role !== 'self'),
     role: 'self',
   });
 
-  expect(ranked[0]!.hex).toBe('#109BB4');
+  expect(ranked[0]!.hex).toBe('#E09A6D